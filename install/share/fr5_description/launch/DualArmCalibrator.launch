<?xml version="1.0"?>
<launch>
    <!-- 加载 ZLab 双臂 MoveIt 配置 -->
    <!-- <include file="$(find fr2_moveit_config)/launch/move_group.launch">
        <arg name="allow_trajectory_execution" value="true" />
        <arg name="fake_execution" value="false" />
        <arg name="info" value="true" />
    </include> -->

    <!-- 可选：启动 AprilTag 检测（如果未在其他地方启动） -->
    <!-- 
  <include file="$(find apriltag_ros)/launch/continuous_detection.launch">
    <arg name="camera_name" value="camera" />
    <arg name="image_topic" value="image_rect" />
  </include>
  -->

    <!-- 标定程序节点 -->
    <node name="dual_arm_calibrator" pkg="fr5_description" type="dual_arm_calibrator"
        output="screen">
        <!-- AprilTag 设置 -->
        <param name="apriltag_topic" value="/tag_detections" />
        <param name="arm1_tag_id" value="201" />
        <param name="arm2_tag_id" value="202" />

        <!-- MoveIt 组名称 - 根据 FR5V6 双臂配置修改 -->
        <param name="arm1_group" value="arm1" />
        <param name="arm2_group" value="arm2" />

        <!-- 标定位姿 - 需要根据实际机械臂调整 [x, y, z, qx, qy, qz, qw] -->
        <rosparam param="arm1_calibration_pose">[0.054, 0.237, 0.423,-0.586, 0.400, -0.441, 0.550]</rosparam>
        <rosparam param="arm2_calibration_pose">[0.095, -0.233, 0.428,-0.493, 0.524, -0.490, 0.492]</rosparam>

        <!-- 结果保存路径 -->
        <param name="calibration_result_path"
            value="$(find fr5_description)/config/dual_arm_calibration.yaml" />
        <param name="required_measurements" value="10" />
        <param name="measurement_interval" value="0.5" />
    </node>

    <!-- 可选：RViz 可视化 -->
    <!-- <node name="rviz" pkg="rviz" type="rviz"
        args="-d $(find fr2_moveit_config)/launch/moveit.rviz" required="true" /> -->
</launch>