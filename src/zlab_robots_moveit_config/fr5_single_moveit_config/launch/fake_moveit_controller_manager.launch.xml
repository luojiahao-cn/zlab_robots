<launch>

  <!-- execute the trajectory in 'interpolate' mode or jump to goal position in 'last point' mode -->
  <arg name="fake_execution_type" default="interpolate" />
  
  <!-- Arm ID parameter -->
  <arg name="arm_id" default="arm1" doc="Arm ID to use"/>

  <!-- Set the param that trajectory_execution_manager needs to find the controller plugin -->
  <param name="moveit_controller_manager" value="moveit_fake_controller_manager/MoveItFakeControllerManager"/>

  <!-- The rest of the params are specific to this plugin -->
  <!-- Load fake controller configuration from YAML with parameter substitution -->
  <rosparam command="load" file="$(find fr5_single_moveit_config)/config/fake_controllers.yaml" subst_value="true"/>

</launch>
