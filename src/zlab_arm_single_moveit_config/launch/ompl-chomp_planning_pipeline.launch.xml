<launch>
  <!-- Arm ID parameter -->
  <arg name="arm_id" default="arm1" doc="Arm ID to use"/>
  
  <!-- load OMPL planning pipeline, but add the CHOMP planning adapter. -->
  <include file="$(find zlab_arm_single_moveit_config)/launch/ompl_planning_pipeline.launch.xml">
    <arg name="planning_adapters"
         default="default_planner_request_adapters/LimitMaxCartesianLinkSpeed
                  default_planner_request_adapters/AddTimeParameterization
                  default_planner_request_adapters/FixWorkspaceBounds
                  default_planner_request_adapters/FixStartStateBounds
                  default_planner_request_adapters/FixStartStateCollision
                  default_planner_request_adapters/FixStartStatePathConstraints
                  chomp/OptimizerAdapter"
                  />
    <arg name="arm_id" value="$(arg arm_id)" />
  </include>

  <!-- load chomp config -->
  <rosparam command="load" file="$(find zlab_arm_single_moveit_config)/config/chomp_planning.yaml" />

  <!-- override trajectory_initialization_method: Use OMPL-generated trajectory -->
  <param name="trajectory_initialization_method" value="fillTrajectory"/>
</launch>
